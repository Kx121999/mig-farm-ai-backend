# MIG FARM AI — إصلاح فهم الجملة وتشخيص النبات V31.1

هذه حزمة تحديث فوق النسخة الموجودة، وليست مشروعًا جديدًا. لا تمسح مجلدات المعرفة أو النسخ القديمة. ارفع الملفات بنفس المسارات إلى جذر مستودع GitHub، ووافق على الاستبدال.

## ما تم إصلاحه

- استبدال القيمة الافتراضية غير المحمولة `gpt-5.6` بـ `gpt-5-mini` في مسارات الفهم والرد والمراجعة.
- تحويل أي قيمة قديمة تساوي `gpt-5.6` تلقائيًا إلى `gpt-5-mini`، حتى لو بقيت في Vercel.
- استخراج المحصول والعَرَض من الجملة كاملة، مثل: `زيتون` + `اصفرار الأوراق`.
- قبول المحاصيل غير الموجودة في القائمة القديمة بدل تجاهلها.
- مسار تشخيص احتياطي يعمل عند تعطل OpenAI أو انتهاء الرصيد أو انتهاء المهلة.
- الرد يعترف بالمعلومات التي قالها العميل، ولا يطلب اسم المحصول مرة ثانية.
- إصلاح تعارض كان يعتبر رسوم الشحن الموثقة سعر منتج غير موثق.
- إظهار `model` وحالة إصلاح المحصول/العَرَض في `/api/health`.

## الملفات التي تستبدلها في GitHub

- `.env.example`
- `api/chat.js`
- `api/health.js`
- `lib/dialogue.js`
- `lib/final_production_os.js`
- `lib/llm_first_orchestrator_v31.js`
- `lib/neural_agent.js`
- `tests/v31_llm_first_orchestrator.mjs`
- `UPLOAD_FINAL_PRODUCTION_OS_AR.txt`

## الرفع

1. فك ضغط الحزمة.
2. افتح مستودع `mig-farm-ai-backend` على GitHub.
3. اختر **Add file → Upload files**.
4. اسحب محتويات الحزمة كما هي، مع مجلدي `api` و`lib`، إلى جذر المستودع.
5. نفّذ **Commit changes** على فرع `main`.
6. انتظر حتى يصبح آخر Deployment في Vercel بحالة **Ready**.

لا ترفع مفتاح OpenAI إلى GitHub.

## متغيرات Vercel

وجود `OPENAI_API_KEY` يكفي. وللتثبيت الصريح يمكن إضافة القيم التالية لكل Production:

```text
OPENAI_MODEL=gpt-5-mini
OPENAI_INTENT_MODEL=gpt-5-mini
OPENAI_CRITIC_MODEL=gpt-5-mini
```

الحزمة تصلح تلقائيًا القيمة القديمة `gpt-5.6`، لذلك لن يتوقف المسار إذا نسيت تغييرها.

## اختبار ما بعد الرفع

افتح:

```text
https://mig-farm-ai-backend.vercel.app/api/health
```

وتأكد من ظهور:

```json
"model": "gpt-5-mini",
"crop_symptom_recovery": true
```

بعدها امسح محادثة الشات القديمة وجرب:

```text
عندي شجرة الزيتون الورق لونه اصفرار
```

الرد الصحيح يجب أن يذكر **الزيتون** و**اصفرار الأوراق**، ثم يرتب احتمالات آمنة ويسأل سؤال تشخيص واحد. يجب ألا يطلب اسم المحصول مرة أخرى.

إذا بقي الرد القديم، افتح آخر Deployment في Vercel وتأكد أنه مربوط بآخر Commit، ثم نفّذ تحديثًا قويًا للصفحة `Ctrl + F5` وابدأ محادثة جديدة.

## التحقق المنفذ قبل التسليم

- فحص بناء JavaScript: ناجح.
- اختبار فهم جملة الزيتون والاصفرار بدون OpenAI: ناجح.
- اختبار الطلب الكامل `/api/chat`: ناجح.
- اختبارات V31 للمسار الدلالي وOpenAI: ناجحة.
- اختبارات FINAL_PRODUCTION_OS: ناجحة.
- اختبار مهندس الزراعة: ناجح.
- فحص الإصدار: `155` ملف JavaScript، و`200025` سجل معرفة، و`1320/1320` تقييم نهائي — ناجح.

