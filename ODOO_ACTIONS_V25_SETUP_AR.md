# إعداد إجراءات Odoo الآمنة داخل V27

الإجراءات المباشرة مقفولة افتراضيًا. تظل المحادثة والبحث شغالين، لكن إنشاء مسودة عرض سعر أو Lead لن يعمل حتى تضيف المتغيرات في Vercel.

## متغيرات Vercel

انسخ أسماء المتغيرات من `.env.example` وأضف القيم الحقيقية داخل **Vercel → Project Settings → Environment Variables**:

- `ODOO_ACTIONS_ENABLED=true`
- `ODOO_ACTION_URL=` ثم رابط Odoo
- `ODOO_DB=` ثم اسم قاعدة البيانات
- `ODOO_USERNAME=` ثم مستخدم خدمة AI
- `ODOO_API_KEY=` ثم المفتاح الحقيقي

لا تضع أي مفتاح داخل ملف واجهة Odoo أو GitHub. المفاتيح تبقى في Vercel فقط.

## صلاحيات مستخدم الخدمة

- قراءة المنتجات والمتغيرات.
- قراءة وإنشاء جهات الاتصال.
- إنشاء CRM Leads.
- قراءة وإنشاء عروض الأسعار في حالة Draft.
- قراءة طلبات البيع للتحقق من الحالة.

لا تمنحه صلاحيات تأكيد طلب البيع، تحصيل الدفع، ترحيل الفواتير، تعديل المخزون، الحذف، أو صلاحيات الإدارة.

## ضمانات التنفيذ

- لا يتم أي تعديل قبل نموذج تأكيد صريح داخل المحادثة.
- رقم الإجراء محفوظ على الخادم، فلا يمكن للمتصفح اختراع إجراء جديد.
- إعادة إرسال نفس التأكيد لا تنشئ مسودة ثانية.
- عرض السعر يظل Draft ولا يتم استدعاء `action_confirm`.
- حالة الطلب لا تظهر إلا بعد تطابق مرجع الطلب مع رقم الهاتف المسجل.
- أي فشل يوقف العملية ولا يدّعي أن طلبًا أو دفعًا تم.

بعد إضافة المتغيرات، أعد نشر المشروع ثم افتح `/api/health` وتأكد أن `autonomous_actions.gateway.configured` تساوي `true`.

