# MIG FARM AI V30 — Neural Autonomous Customer OS

الإصدار: `30.0.0`  
الوضع: `neural_autonomous_customer_os_v30`

## التطوير الرئيسي

تحولت طبقة المحادثة من مجرد اختيار رد إلى نظام تشغيل للعميل يخطط كل دور قبل التنفيذ. النظام يحدد مقصد العميل والمخاطر والأدلة المطلوبة، ثم يوزع العمل على الوكلاء المتخصصين المناسبين، ويضع الرد النهائي أمام بوابة ثقة قبل إرساله.

### 1. مخطط المهمة المستقل

- يبني خطة منفصلة لكل رسالة بدل تشغيل كل الأدوات عشوائيًا.
- يختار وكلاء المحادثة وحقيقة المنتج والزراعة وبيانات الشركة والتجارة والسلامة والجودة حسب السؤال.
- يحدد الأدوات المسموح بها وعدد استدعاءاتها والأدلة المطلوبة.
- يستخدم المسار العصبي عند توفر OpenAI وكانت المهمة تحتاجه.
- يرجع للمسار الحتمي تلقائيًا عند غياب المزود أو فشله.

### 2. ملف العميل الرقمي الآمن

- يحتفظ فقط بالحقائق التي ذكرها العميل صراحة: الإمارة والمحصول وطريقة الزراعة والميزانية والكمية والهدف والمنتج ومعيار الاختيار.
- لا يخزن نص المحادثة الخام أو الهاتف أو البريد أو بيانات الطلب الخاصة أو الدفع أو الصفات الحساسة.
- الحقائق محدودة العدد وتنتهي بعد عدد محدد من أدوار المحادثة.

### 3. بوابة الثقة قبل الإرسال

- تقيس اكتمال الرد والأدلة والمخاطر قبل عرضه.
- تختار بين: إجابة، سؤال توضيحي، تحويل لمختص، أو حجب الادعاء غير الآمن.
- تحجب الجرعات غير الموثقة وادعاءات تنفيذ الطلب أو الدفع بدون إثبات.
- تحافظ على الردود الصحيحة ولا تعيد صياغة الحقائق الموثقة بلا داعٍ.

### 4. تعلم مغلق وآمن

- يتعلم من نتيجة الدور: هل أُجيب، هل احتاج توضيحًا، هل حُل السياق، وهل وُجد دليل.
- يخزن إحصاءات مجمعة وبصمات مشفرة فقط.
- لا يخزن رسائل العملاء أو الردود الخام أو معرّفات الجلسات أو البيانات الشخصية.

### 5. التشغيل والإدارة

- أضيفت حالة V30 إلى `/api/health`.
- أضيفت حالة العقل المستقل وبوابة الثقة وملف العميل والتعلم إلى لوحة الإدارة.
- أضيف تقرير تقييم V30 إلى لوحة الإدارة.
- واجهة Odoo الجديدة تستخدم مفاتيح تخزين V30 حتى لا تحمل حالة قديمة تالفة.

## قاعدة المعرفة الحالية

قاعدة المعرفة ذات 400MB تظل كما هي على GitHub. حزمة V30 هي ترقية للكود والاختبارات والواجهة فقط؛ لا تحذف ولا تعيد رفع مجلدات المعرفة.

## التحقق المنفذ

- تقييمات V30: `65/65`.
- اختبار V30 متعدد الأدوار: نجاح سؤال الفروع ثم الرد `3ain`.
- توافق V29 الحواري: ناجح.
- عقد API الخاص بمنصة V28: ناجح.
- حماية لوحة الإدارة: ناجحة.
- فحص الإصدار الكامل: `144` ملف JavaScript و`28` ملف JSON و`200025` سجل معرفة، والنتيجة PASS.

## متطلبات التشغيل

- لا يحتاج V30 إلى متغير جديد إلزامي.
- إذا كان `OPENAI_API_KEY` موجودًا يستخدم النظام المسار العصبي عند الحاجة.
- إذا لم يكن موجودًا يظل النظام يعمل بالمسار الحتمي القوي.
- تظل `ODOO_ACTIONS_ENABLED=false` إلى أن تقرر ربط تنفيذ أوامر Odoo لاحقًا.
