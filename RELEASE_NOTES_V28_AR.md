# MIG FARM AI V28 — Enterprise Autonomous Intelligence Platform

## التطوير الرئيسي

V28 ينقل المشروع من شات كبير إلى منصة ذكاء قابلة للإدارة والتوسع. تم الحفاظ على جميع طبقات V27، بما فيها 400 MB معرفة محلية وذاكرة العميل وحماية سياق المنتج، ثم إضافة طبقة مؤسسية فوقها.

## الجديد

- Enterprise Supervisor يخطط للطلبات ويربطها بمحركات متخصصة ويطبق بوابة جودة قبل الإرسال.
- محركات متخصصة لحقائق الشركة، حقيقة المنتج، الترشيح، البيع، الزراعة، السلامة، الصور، التجارة وعمليات Odoo.
- ردود منظمة عبر `reply_blocks` مع الحفاظ على صياغة طبيعية مناسبة للشات.
- Hybrid Retrieval يجمع OpenAI Vector Store مع قاعدة V27 المحلية، ويعود تلقائيًا للمحلي عند عدم تفعيل الخدمة الخارجية.
- لوحة إدارة عربية على `/admin` بتصميم متجاوب للديسكتوب والموبايل.
- جلسة إدارة HttpOnly موقعة، مقارنة آمنة للرموز، منع الكاش والفهرسة وطلبات المواقع الخارجية.
- Telemetry اختيارية مستمرة عبر Upstash Redis مع بديل مؤقت، وبدون حفظ الرسائل الخام.
- تقرير إدارة قابل للتنزيل.
- Manifest مؤسسي، مزامنة تدريجية بالـSHA-256، واختبارات تقييم ذهبية.

## إصلاحات مهمة

- إصلاح اكتشاف ادعاءات الجرعة العربية مثل `10 مل` حتى لا تمر جرعة غير موثقة بسبب حدود الكلمات العربية.
- عزل سياق المنتج أو الجرعة القديم عند أسئلة الهوية أو الفروع أو الكلام الاجتماعي.
- منع عرض مصطلحات تنفيذية داخل رد العميل مثل `Odoo Live` واستبدالها بصياغة طبيعية.
- حفظ أسرار لوحة التحكم على الخادم فقط وعدم تخزين رمز الدخول في Local Storage.
- إصلاح فشل Vercel الناتج عن ضم قاعدة المعرفة 400 MB داخل `api/chat.js`.
- إبقاء `manifest.json` و`router.json` فقط داخل Function، وتحميل حزمة معرفة واحدة عن بُعد لكل سؤال.
- التحقق من حجم الحزمة و`SHA-256` قبل استخدامها، مع كاش محدود ومنع تكرار التحميل المتزامن.
- تصحيح صيغة `includeFiles` في `vercel.json` لتكون String صالحة لكل Function.
- إضافة `.vercelignore` لمنع إدخال `knowledge_v27/packs` في حزمة Vercel، مع إبقاء الملفات كاملة في GitHub.
- إضافة اختبار انحدار يحاكي النقل البعيد ويتحقق من الرابط والاسترجاع والكاش وسلامة الحزمة.

## التوافق

- واجهة Odoo الجديدة: `ODOO_CHAT_UI_V28_ENTERPRISE_AUTONOMOUS_INTELLIGENCE_PLATFORM.txt`.
- Odoo Actions معطلة افتراضيًا ولا يتم تنفيذ أي طلب أو عرض سعر بدون الإعداد والتأكيد.
- قاعدة V27 محفوظة كاملة في GitHub/تخزين ملفات بعيد، بينما حزمة Vercel نفسها تظل صغيرة وآمنة للنشر.
